import 'package:flutter/material.dart';
class AppColors {
  static const primary = Color(0xFFEA7A1A);     // warm orange (CTA like "Add to Cart")
  static const background = Color(0xFFF7F4EF);  // soft warm beige background
  static const card = Color(0xFFFFFFFF);        // clean white cards
  static const surface = Color(0xFFFFF9F3);     // soft inner panels

  static const textPrimary = Color(0xFF1F1F1F); // strong readable text
  static const textMuted = Color(0xFF8A8A8A);   // descriptions

  static const accent = Color(0xFFB85C1A);      // darker orange for highlights
  static const error = Color(0xFFEF4444);
}